{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; [ -f \"$f\" ] && pnpm exec prettier --write --ignore-unknown \"$f\" >/dev/null 2>&1; [[ \"$f\" =~ \\.(ts|tsx|js|jsx|mjs|cjs)$ ]] && pnpm exec eslint --fix \"$f\"; } || true"
          }
        ]
      }
    ]
  }
}
